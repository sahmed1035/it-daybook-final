{
  "name": "it-daybook-final",
  "version": "0.1.0",
  "homepage": "http://sahmed1035.github.io/it-daybook-final",
  "private": true,
  "dependencies": {
    "materialize-css": "^1.0.0",
    "moment": "^2.24.0",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-moment": "^0.9.6",
    "react-redux": "^7.1.1",
    "react-scripts": "3.2.0",
    "redux": "^4.0.4",
    "redux-devtools-extension": "^2.13.8",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "json-server": "json-server --watch db.json --port 5000",
    "dev": "concurrently \"npm start\" \"npm run json-server\" ",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run -d build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "gh-pages": "^2.1.1",
    "json-server": "^0.15.1"
  },
  "proxy": "http://localhost:5000",
  "description": "An internal app for the IT department to keep records of daily issues and fixes. Each record (log) has an id, a message, the technician that added it, date and time. A log could be edit and set to attention needed. Also the technicians could be added or removed from the system. IT department is also able to search the logs.",
  "main": "App.js",
  "author": "Syeda Ahmed",
  "license": "MIT"
}
